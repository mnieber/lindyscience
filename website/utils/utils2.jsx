// @flow

import * as React from "react";
import { isNone as isNone_ } from "utils/utils";
import { create_uuid as create_uuid_ } from "utils/utils";

export const isNone = isNone_;
export const create_uuid = create_uuid_;
